**Summary**

Tests run for this feature and results.

## Matrix
- Unit tests: PASS/FAIL
- Integration: PASS/FAIL
- E2E: PASS/FAIL
- Static analysis: PASS/FAIL

## Artifacts
Links to CI job artifacts (coverage, screenshots, logs). Include raw URLs to the CI provider job/artifact.

Example placeholders:
- Coverage report: <CI_JOB_URL>/artifacts/coverage-report
- Sonar dashboard: https://sonarcloud.io/project?id=ORG_PROJECT_KEY

Local setup:
- You can create a `.env` file in the repo root containing `SONAR_TOKEN` and `CI_JOB_URL` for local testing of CI steps. Example:

```
SONAR_TOKEN=RANDOM_SONAR_TOKEN_ABC123
CI_JOB_URL=https://ci.example.com/job/12345
```

## Known issues

List failures and mitigations.
